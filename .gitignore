# Secrets — .env is materialized by `op inject -i .env.op > .env` on mise enter.
# NEVER commit it. .env.op holds only 1Password references or safe literals and IS committed.
.env
.env.*
!.env.op
!.env.example

# Per-deployment site config — a team's own endpoint/email, like .env. Not shipped.
# The committed template is apps/web/config.example.js.
apps/web/config.js

# Per-team filled outreach (a coach's real name/school/URL) — emails, social posts, etc.
# The committed templates are recruiting/**/*.md; personal *.filled.md copies stay local, like config.js.
recruiting/**/*.filled.md

# Personal outreach artifacts — exported mail threads, saved correspondence. These carry a real
# name, address, phone and thread contents; this repo is public. Same posture as *.filled.md.
recruiting/**/*.pdf
!recruiting/flyer/lego-league-flyer.pdf

# The kit ships its OWN Claude Code tooling (a machine-global gitignore excludes .claude/).
# Re-include only the two product artifacts — the orchestrator agent and the deploy skill —
# and keep everything else in .claude/ ignored (local settings, the BMAD skill cache, etc.).
.claude/*
!.claude/agents/
.claude/agents/*
!.claude/agents/master-builder.md
# .claude/skills is the tracked alias to .agents/skills (the one canonical skill root);
# deploy-team-site is tracked there, force-added under the generated-projection ignore.
!.claude/skills
.hallmark/

# AGENTS.md is canonical; these two are symlinks generated by `mise run link-agentfiles` (§7).
CLAUDE.md
GEMINI.md

# Canonical agent config lives in .agents; generated skill projections stay local.
/.agents/skills
